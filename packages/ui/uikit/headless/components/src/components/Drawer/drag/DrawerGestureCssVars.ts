export enum DrawerGestureCssVars {
    swipeMovement = '--swipe-movement',
    snapPointTransform = '--snap-point-transform',
    snapPointMax = '--snap-point-max',
    snapPointIndex = '--snap-point-index'
}
